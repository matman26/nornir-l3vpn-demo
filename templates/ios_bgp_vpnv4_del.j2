{% for neighbor in host.data.bgp.peers %}
{% set bgp_asn = host.data.bgp.asn %}
router bgp {{ bgp_asn }}
 no neighbor {{ neighbor.ip }} remote-as {{ bgp_asn }}
 no neighbor {{ neighbor.ip }} update-source Loopback 0
 address-family vpnv4
  no neighbor {{ neighbor.ip }} activate
!
{% endfor %}
